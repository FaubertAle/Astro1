---
const { blogsList } = Astro.props;
---

<ul class="links">
    {blogsList.map((post)=> <li><a href={post.url}>{post.frontmatter.title}</a></li> )}
</ul>

<style>
    .links {
        li {
            a {
                text-decoration: none;
                &:checked {
                    color: red;
                }
            }
        }
    }
</style>